import {supabase} from './supabase.js';
const $=s=>document.querySelector(s);const esc=v=>String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));
async function load(){const {data,error}=await supabase.from('intelligence_tools').select('name,description,tool_type,required_permission,input_schema,is_active').eq('is_active',true).order('name');if(error){console.error(error);return}$('#tools').innerHTML=(data||[]).map(t=>`<article class="project-card"><p class="eyebrow">${esc(t.tool_type)}</p><h3>${esc(t.name)}</h3><p>${esc(t.description||'No description provided.')}</p><div class="project-meta"><span>${t.required_permission?`Permission: ${esc(t.required_permission)}`:'No permission specified'}</span><span>Active</span></div></article>`).join('');$('#empty').hidden=!!data?.length}load();